configurations {
    jar.archiveName = 'Visualization-' + getVersion() + '.jar'
}

dependencies {
    implementation project(":core")
    compile project(":core")
}

jar {
    manifest {
        attributes(
                'Main-Class': "de.daschi.visualization.application.Application"
        )
    }
}